const forma = 'quadrado'
const altura = 10
const largura = 20
let area;

if (forma === 'quadrado') {
    area = altura * largura
} else (
    area = (altura * largura) / 2
)

console.log(area)